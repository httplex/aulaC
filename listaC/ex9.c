#include <stdio.h>
#include <stdlib.h>

int main(){
    int numero, cont = numero*cont;
    printf("Informe um número: ");
    scanf("%d", &numero);

    for(int cont = 1; cont <=10; cont++){
        printf("%dx%d = %d \n", cont, numero, cont * numero);
        
    }
}